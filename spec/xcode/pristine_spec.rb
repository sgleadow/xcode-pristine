require "spec_helper"

RSpec.describe Xcode::Pristine do
  it "has a version number" do
    expect(Xcode::Pristine::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
